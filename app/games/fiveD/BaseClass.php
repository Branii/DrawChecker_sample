<?php 
require_once(__DIR__ . "/Utils.php");
class BaseClass { 

    //win or loss checking logic goes here

    // public static function group120(Array $drawNumber, Int $slice) : Bool {
    //     return Utils::findPattern([1,1,1,1,1], $drawNumber, 0, $slice); // no repeat
    // }

    // public static function group60(Array $drawNumber, Int $slice) : Bool {
    //     return Utils::findPattern([2,1,1,1], $drawNumber, 0, $slice); // 1 pair, 3 diff
    // }

    // public static function group30(Array $drawNumber, Int $slice) : Bool {
    //     return Utils::findPattern([2,2,1], $drawNumber, 0, $slice); // 2 pair, 1 diff
    // }

    // public static function group20(Array $drawNumber, Int $slice) : Bool {
    //     return Utils::findPattern([3,1,1], $drawNumber, 0, $slice); // 1 triple, 2 diff
    // }

    // public static function group10(Array $drawNumber, Int $slice) : Bool {
    //     return Utils::findPattern([3,2], $drawNumber, 0, $slice); // 1 triple, 1 pair
    // }

    // public static function group5(Array $drawNumber, Int $slice) : Bool {
    //     return Utils::findPattern([4,1], $drawNumber, 0, $slice); // 1 quad, 1 diff
    // }

}
